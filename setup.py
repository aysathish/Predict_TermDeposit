from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Predict if a client will subscribe to a term deposit based on marketing campaign',
    author='aysathish',
    license='MIT',
)
